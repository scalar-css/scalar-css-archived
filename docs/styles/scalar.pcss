@scalar reset;
@scalar debug;
@scalar utilities;

@import './lib/display.pcss';
@import './lib/flex.pcss';
@import './lib/margins.pcss';

[class*='max-w-'] {
  max-width: var(--max-w);
}

[class*='items-'] {
  align-items: var(--items);
}

.items-center {
  --items: center;
}
